// Empty stub for @vercel/og — prevents wrangler/esbuild from bundling
// the OG image runtime (resvg.wasm + yoga.wasm + satori) which this project does not use.
// eslint-disable-next-line import/no-anonymous-default-export -- intentional empty stub
export default {};
